---
name: wizard
description: Run a wizard template to guide a multi-step command-line flow.
disable-model-invocation: true
---

按 `template.sh` 运行一个交互式 wizard，用于把多步骤命令行流程拆成清晰步骤。

不要把 wizard 当成长期产品界面；它是临时引导层，目标是让用户安全完成一次流程，并留下可复用命令。
